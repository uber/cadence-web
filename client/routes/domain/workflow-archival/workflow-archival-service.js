import { http } from '~helpers';

export default () => {
  return {
    fetchArchivalRecord: ({ workflowId, runId }) => {
      // TODO
      // return http(window.fetch, `/api/domains/${domainName}`);
    },
  };
};
