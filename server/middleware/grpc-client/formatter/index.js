const formatDefault = require('./format-default');
const formatDomain = require('./format-domain');
const formatListDomains = require('./format-list-domains');

module.exports = {
  formatDefault,
  formatDomain,
  formatListDomains,
};